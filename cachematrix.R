## Creates a cache matrix and computes its inverse

## Creates a cache matrix
makeCacheMatrix <- function(x = matrix()) {amartix << matrix (c(1,2,3,4), nrow=2, ncol=2))
                                           amatrix$get()  
                                           cacheSolve(amatrix) # Returns cached matrix inverse 

}


## Write a short comment describing this function

cacheSolve <- function(x, ...) { Solve(x)
        ## Return a matrix that is the inverse of 'x'
}
